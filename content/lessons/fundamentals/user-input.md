# إدخال المستخدم

في هذا الدرس، سنتعلم كيفية الحصول على إدخال من المستخدم في روبي باستخدام أمر `gets`. هذا يسمح لبرامجنا بالتفاعل مع المستخدمين وتلقي بيانات منهم.

## أمر gets

أمر `gets` يوقف البرنامج وينتظر حتى يكتب المستخدم شيئاً ويضغط Enter:

```ruby
puts "ما اسمك؟"
name = gets
puts "مرحباً #{name}"
```

لكن هناك مشكلة! عندما يضغط المستخدم Enter، يُضاف سطر جديد (`\n`) في نهاية الإدخال:

```ruby
puts "ما اسمك؟"
name = gets
puts name.inspect  # "أحمد\n" - لاحظ \n في النهاية!
```

## استخدام chomp لإزالة السطر الجديد

لحل هذه المشكلة، نستخدم دالة `chomp` التي تزيل السطر الجديد من نهاية النص:

```ruby
puts "ما اسمك؟"
name = gets.chomp
puts "مرحباً #{name}!"
# إذا أدخل المستخدم: أحمد
# الناتج: مرحباً أحمد!
```

### الفرق بين gets و gets.chomp

```ruby
# بدون chomp
input = gets
puts "الإدخال: [#{input}]"
puts "الطول: #{input.length}"
# إذا أدخل المستخدم: مرحبا
# الناتج:
# الإدخال: [مرحبا
# ]
# الطول: 6 (5 حروف + سطر جديد)

# مع chomp
input = gets.chomp
puts "الإدخال: [#{input}]"
puts "الطول: #{input.length}"
# إذا أدخل المستخدم: مرحبا
# الناتج:
# الإدخال: [مرحبا]
# الطول: 5
```

## ملاحظة مهمة: الإدخال في المتصفح

في هذه الدورة التفاعلية، نستخدم ruby.wasm لتشغيل روبي في المتصفح. بما أنه لا يوجد "طرفية" حقيقية في المتصفح، نستخدم **نظام إدخال محاكى**.

**كيف يعمل؟**
1. اكتب القيم التي تريد إدخالها في **حقل الإدخال** أعلى المحرر
2. كل سطر يمثل إدخالاً منفصلاً لكل `gets`
3. عند تشغيل الكود، سيقرأ `gets` السطر التالي من حقل الإدخال

**مثال:**
إذا كان حقل الإدخال يحتوي على:
```
أحمد
25
```

والكود هو:
```ruby
name = gets.chomp
age = gets.chomp
puts "اسمك: #{name}"
puts "عمرك: #{age}"
```

سيكون الناتج:
```
اسمك: أحمد
عمرك: 25
```

## أمثلة عملية

### مثال 1: تحية شخصية

```ruby
puts "أهلاً! ما اسمك؟"
name = gets.chomp
puts "سعيد بلقائك يا #{name}!"
```

### مثال 2: جمع معلومات

```ruby
puts "ما اسمك؟"
name = gets.chomp

puts "ما عمرك؟"
age = gets.chomp

puts "ما مدينتك؟"
city = gets.chomp

puts "مرحباً #{name}!"
puts "عمرك #{age} سنة وتسكن في #{city}."
```

### مثال 3: سؤال وجواب

```ruby
puts "ما هي لغة البرمجة المفضلة لديك؟"
language = gets.chomp

if language.downcase == "ruby"
  puts "اختيار ممتاز! 💎"
else
  puts "#{language} لغة جيدة، لكن جرب روبي أيضاً!"
end
```

## gets يُرجع دائماً نصاً

تذكر أن `gets` يُرجع دائماً **نصاً** (String)، حتى لو أدخل المستخدم رقماً:

```ruby
puts "أدخل رقماً:"
input = gets.chomp
puts input.class  # String

# للعمل بالأرقام، تحتاج تحويل النص
number = input.to_i  # سنتعلم هذا في الدرس القادم
```

## دوال مساعدة مع gets

### تنظيف الإدخال

```ruby
puts "أدخل اسمك:"
name = gets.chomp.strip  # يزيل السطر الجديد والمسافات الإضافية

puts "أدخل بريدك الإلكتروني:"
email = gets.chomp.downcase.strip  # تنظيف وتحويل لأحرف صغيرة
```

### التحقق من الإدخال الفارغ

```ruby
puts "أدخل اسمك:"
name = gets.chomp

if name.empty?
  puts "لم تدخل اسماً!"
else
  puts "مرحباً #{name}!"
end
```

## جدول ملخص

| الأمر | الوصف | مثال |
|-------|-------|------|
| `gets` | قراءة سطر من الإدخال (مع `\n`) | `input = gets` |
| `gets.chomp` | قراءة سطر بدون سطر جديد | `input = gets.chomp` |
| `gets.chomp.strip` | قراءة وإزالة المسافات الإضافية | `input = gets.chomp.strip` |
| `gets.chomp.downcase` | قراءة وتحويل لأحرف صغيرة | `input = gets.chomp.downcase` |

## نصائح مهمة

1. **استخدم دائماً `chomp`** مع `gets` لتجنب مشاكل السطر الجديد
2. **تذكر أن الإدخال نص** - ستحتاج تحويله إذا أردت رقماً
3. **نظّف الإدخال** باستخدام `strip` للتخلص من المسافات الزائدة
4. **تحقق من الإدخال الفارغ** قبل استخدامه
5. **في المتصفح**: استخدم حقل الإدخال المحاكى لإدخال القيم مسبقاً

## مثال شامل: بطاقة تعريف

```ruby
puts "=== إنشاء بطاقة تعريف ==="
puts ""

puts "الاسم الأول:"
first_name = gets.chomp.strip

puts "اسم العائلة:"
last_name = gets.chomp.strip

puts "المدينة:"
city = gets.chomp.strip

puts ""
puts "╔══════════════════════════╗"
puts "║      بطاقة تعريف         ║"
puts "╠══════════════════════════╣"
puts "║ الاسم: #{first_name} #{last_name}"
puts "║ المدينة: #{city}"
puts "╚══════════════════════════╝"
```

---

## التمرين

الآن حان دورك! اكتب برنامجاً يطلب من المستخدم اسمه ثم يحييه بشكل شخصي.

**ملاحظة:** أدخل اسماً في حقل الإدخال أعلى المحرر قبل تشغيل الكود.
